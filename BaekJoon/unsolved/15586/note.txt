# 
알고리즘 분류 : 오프라인 쿼리
잘 생각이 안나므로, 나중에 생각해보기..

# 01
---
1 <= N <= 100,000   // 영상 번호
1 <= Q <= 100,000   // 질문 수

1 <= p, q <= N      // 영상 번호
1 <= r <= 1,000,000,000 // 유사도

인접행렬 ? 
10,000,000,000 int -> 40GB x

인접리스트
(N-1) * 2

---
특정 영상까지 연결되어 있으면, 그 경로의 간선들 중 최솟값

유사도는 높을수록 좋은데... 

---
1 -> 2 : 4
1 -> 3 -> 2
  6    7
이라면, 1 -> 2 는 6? or 4?
지문)
경로가 반드시 하나 존재 -> 하나만 존재?
그 경로의 모든 연결들의 USADO 중 최솟값 
-> 그 경로는 단수이므로 하나만 존재하고 그 하나의 경로에서 최소 간선을 의미하는듯

따라서 위와 같은 경우는 없음


