# 일단 아래대로 하니까 안되긴 했음.
평균?
최대 M번, 
k번 자를 때, N + k 개의 조각이 생김, 
모든 조각이 전체 케이크의 N+k 분할 되면 최소임

2
1 3
1
같은 경우, 
1번 자르면 1 1.5 1.5 로 잘리지만
2번 자르면 1 1 2 -> 1 1 1 1 로 자를 수 있음



# 생각 1
3
7 11 13
10

7 11 13
중, 무조건 큰 요소 선택 하긴 해야함
나눈다 -> 작아진다.
큰 값을 선택하지 않으면 max는 그대로 and 더 작아질 가능성은 있음
-> 손해

but 큰 값을 선택하면, max 값은 확실히 감소
max heap

나머지 수의 범위
7 ~ 11
13 / 2 = 6.5

범위 안에 들지 않으면 절반이 가장 좋다?

만약 범위 안에 들면? 그래도 절반?



    for(i = 0; i <= gHeapSize; i++) printf("%lf ", gHeap[i]); printf("\n");

